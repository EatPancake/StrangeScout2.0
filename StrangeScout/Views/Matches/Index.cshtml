@model IEnumerable<StrangeScout.Models.Matches>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Event)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Points)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Penalties)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Event)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Points)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Penalties)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TeamNumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TeamNumber">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TeamNumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
